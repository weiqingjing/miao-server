spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${mysql.url}
    username: ${mysql.username}
    password: ${mysql.password}

mybatis:
  mapper-locations: classpath:mapper/*.xml

server:
  port: 8081

mini-app:
  appId: wx4d98dc9b3984f23f